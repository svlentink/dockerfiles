# texlive-with-libraries
Document conversion libraries on top of texlive-full

I use this for chaining, this extra layer,
speeds up the rebuilding of the docker image that only contains bash scripts.
